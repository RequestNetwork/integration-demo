# Request Network Integration Demo Client

This directory contains the code for a React app used to list and execute payments via the `rn-test-backend` API locally.

## How to run it
1. `npm install`
2. Copy over the example env file to `.env` - `cp .env.example .env`
3. `npm run dev`
