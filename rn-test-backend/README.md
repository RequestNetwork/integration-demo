# Fastify TypeScript App

A simple TypeScript Fastify application with Hello World endpoints.

## Getting Started

### How to run it

1. `npm install`
2. Copy over the example environment file into a new file `.env` - `cp .env.example .env`
3. Follow our integration guide to get all necessary environment variables via the API portal.
4. `npm run dev:watch`
